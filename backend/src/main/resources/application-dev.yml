spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # Mail Configuration (MailDev)
  mail:
    host: localhost
    port: 1025
    username: ""
    password: ""
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false
    test-connection: false

logging:
  level:
    root: INFO
    com.omnishop360: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
